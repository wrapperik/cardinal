/** Shared weight and delayed-fill timing for every horizontal swipe row. */
export const SWIPE_COMMIT_DISTANCE = 88;
export const SWIPE_MAX_DRAG = 112;
export const DELAYED_FILL_SPRING = {
  damping: 20,
  stiffness: 90,
  mass: 1,
} as const;
